{
  "version": 2,
  "buildCommand": "npm install && tsc",
  "installCommand": "npm install",
  "public": false,
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/health",
      "destination": "/health"
    }
  ]
}
